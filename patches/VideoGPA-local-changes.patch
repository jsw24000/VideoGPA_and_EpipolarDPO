diff --git a/metrics/epipolar.py b/metrics/epipolar.py
index 9977ac6..9f8560d 100644
--- a/metrics/epipolar.py
+++ b/metrics/epipolar.py
@@ -5,9 +5,6 @@ from typing import Optional, Tuple, Dict, Any
 from abc import ABC, abstractmethod
 from kornia.geometry.epipolar import find_fundamental, sampson_epipolar_distance

-from lightglue import LightGlue, SuperPoint
-from lightglue.utils import rbd
-
 from metrics.base import Metric


@@ -80,11 +77,19 @@ class LightGlueMatcher(KeypointMatcher):
         self.device = device if torch.cuda.is_available() else "cpu"

         try:
+            from lightglue import LightGlue, SuperPoint
+            from lightglue.utils import rbd
+
+            self.rbd = rbd
             # Load official SuperPoint and LightGlue
             self.extractor = SuperPoint(max_num_keypoints=2048).eval().to(self.device)
             self.matcher = LightGlue(features='superpoint').eval().to(self.device)
         except Exception as e:
-            print(f"Error loading LightGlue models: {e}")
+            print(
+                "Error loading LightGlue models. Install with: "
+                "python -m pip install 'git+https://github.com/cvg/LightGlue.git'"
+            )
+            print(f"Original error: {e}")
             raise e

     def get_matched_points(self, frame1: np.ndarray, frame2: np.ndarray) -> Tuple[Optional[np.ndarray], Optional[np.ndarray], int, Dict[str, Any]]:
@@ -102,7 +107,7 @@ class LightGlueMatcher(KeypointMatcher):
                 matches01 = self.matcher({'image0': feats0, 'image1': feats1})

                 # Remove batch dimension (kpts: 1xNx2 -> Nx2)
-                feats0, feats1, matches01 = [rbd(x) for x in [feats0, feats1, matches01]]
+                feats0, feats1, matches01 = [self.rbd(x) for x in [feats0, feats1, matches01]]

                 # Get matched point indices
                 matches = matches01['matches'] # indices
@@ -229,4 +234,4 @@ class EpipolarMetric(Metric):
         elif x.max() <= 1.0:
             x = x * 255.0
         x = np.clip(x, 0, 255).astype(np.uint8)
-        return x
\ No newline at end of file
+        return x
diff --git a/pipelines/process_video.py b/pipelines/process_video.py
index 7c4dd59..fdb3ed2 100644
--- a/pipelines/process_video.py
+++ b/pipelines/process_video.py
@@ -4,8 +4,6 @@ import numpy as np
 import torch
 from huggingface_hub.errors import LocalEntryNotFoundError

-from depth_anything_3.api import DepthAnything3
-from depth_anything_3.utils.geometry import affine_inverse, unproject_depth
 from utils import batch_reproject, get_colored_pointcloud, run_model_gpu, sample_uniform_frames
 from vggt.models.vggt import VGGT

@@ -50,6 +48,8 @@ class VideoProcessor:
         return model.to(self.device).eval()

     def _load_da3_model(self, model_name):
+        from depth_anything_3.api import DepthAnything3
+
         try:
             print(f"[DA3] Loading locally: {model_name}")
             model = DepthAnything3.from_pretrained(model_name, local_files_only=True)
@@ -130,6 +130,8 @@ class VideoProcessor:
         return results

     def _build_da3_predictions(self, frames_np_array):
+        from depth_anything_3.utils.geometry import affine_inverse, unproject_depth
+
         prediction = self.model.inference(
             [frames_np_array[i] for i in range(len(frames_np_array))]
         )
